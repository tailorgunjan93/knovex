"""
Knovex Test Suite

Test organisation:
  test_imports.py   — smoke tests: all modules import cleanly, app creates
  test_adapters.py  — unit tests: adapters with stubs (no network, no disk)
  test_reader.py    — unit tests: ReaderService rendering + Q&A with stubs
"""
